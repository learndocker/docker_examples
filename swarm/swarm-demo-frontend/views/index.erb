<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <title>Welcome to the Docker Swarm Demo!</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <h1>Welcome to the Docker Swarm Demo!</h1>
          <p>
            If you see this page, the web app is running inside a Docker container!<br />
            Requests are logged to a PostgreSQL database.
          </p>

          <p>
            This container (<%= Socket.gethostname %>) handled <%= total_requests %> requests since the last start.
          </p>

          <h3>The last 30 requests</h3>
          <table class="table">
            <tr>
              <th>Client IP</th>
              <th>Path</th>
              <th>Container</th>
              <th>Timestamp</th>
            </tr>
            <% requests.each do |r| %>
              <tr>
                <td><%= r.ip %></td>
                <td><%= r.path %></td>
                <td><%= r.host %></td>
                <td><%= r.requested_at %></td>
              </tr>
            <% end %>
        </div>
      </div>
    </div>
    <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
